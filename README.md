
## Core Modules Functionality
- configuration + application settings loading
- model import (stl load, etc)
- slicing model into layers
- region fill algorithm
- toolpath optimization - tsp/solvers
- gcode generation w/ prefix + suffix templates
- debug visualization


## Development Workflow
- for each feature requested, implement tests
- whenever possible, add matplotlib visualization for test cases, which can be enabled for debugging


## Future Planned Features
- Thermoplastic supports generation (travel moves and retraction allowed)
