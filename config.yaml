
### Testing
max_layers_to_process: 4                # For quicker test cycles

### Print settings (shared)

# Width of the toolpath in mm
toolpath_width: 0.4
# Height of each layer in mm
layer_height: 0.5



### Model loader

# the full 3d mesh
visualize_stl_mesh: False



### Slicer

# method for creating polygons from the line segments where triangles intersect slicing plane

#standard: The original polygonize method
#small_buffer: Uses a small buffer to close gaps
#large_buffer: Uses a larger buffer for more aggressive gap closing
#manual_close: Manually connects endpoints that are close to each other 

#polygonization_method: "hybrid"
#polygonization_method: "standard"
polygonization_method: "small_buffer"
#polygonization_method: "large_buffer"
#polygonization_method: "manual_close"


# outer contour polygon for a layer slice 
visualize_layer_contours: True
# debug polygonization failures
visualize_problematic_segments: False



### Region fill

# The region fill path before optimization
visualize_fill_paths: False


### Toolpath optimizer

# path generated by toolpath optimizer
visualize_optimized_paths: False
# distance between end point of prev layer and start point of current
visualize_layer_transitions: False
# Maximum points per segment when splitting paths
max_points_per_segment: 1000
# Scale factor for concorde TSP solver distances
tsp_scale_factor: 100
# Distance between points in smoothed path (mm)
path_smoothing_segment_length: 0.2



### GCode generator
generate_gcode: False                   # Whether to generate GCode output
gcode_flavor: "klipper"                 # GCode flavor, either "klipper" or "marlin"
print_speed: 20                         # Movement speed in mm/s
acceleration: 500                       # Acceleration in mm/s²
